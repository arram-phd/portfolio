This is my portfolio create using sphinx

After running, update the docs folder with the new html files
from the root (/Users/abhneetram/anaconda3/envs/sphinx_portfolio/docs/docs):  
cp -a ./_build/html/. ./docs
